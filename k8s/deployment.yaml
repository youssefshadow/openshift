# le yaml du deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tp-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tp-app
  template:
    metadata:
      labels:
        app: tp-app
    spec:
      containers:
        - name: tp-app
          image: harbor.kakor.ovh/ipim2il/groupe-3/tp-app:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
